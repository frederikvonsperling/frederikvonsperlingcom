import { defineTokens } from "@pandacss/dev";

export default defineTokens.gradients({
  shine: {
    value:
      "linear-gradient(110deg, transparent 8%, {colors.offWhite.50} 18%, transparent 33%)",
  },
});
