import { defineTokens } from "@pandacss/dev";

export default defineTokens.borders({
  offWhite: {
    value: {
      color: "{colors.offWhite.50}",
      style: "solid",
      width: 1,
    },
  },
});
