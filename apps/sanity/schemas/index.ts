import blockContent from "./blockContent";
import tags from "./tags";
import author from "./author";
import article from "./article";

export const schemaTypes = [author, tags, blockContent, article];
